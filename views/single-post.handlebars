<div class="all-posts-container" "post-archive">

        <div class="card">
            <header class="card-header">
                <h1 class="card-header-title">
                    {{title}}
                </h1>
                <button class="card-header-icon" aria-label="more options">
                    <span class="icon">
                        <i class="fas fa-angle-down" aria-hidden="true"></i>
                    </span>
                </button>
            </header>
        </div>

        <div class="card">
            <div class="card-content">
                <div class="content">
                    <p>{{sighting}}</p>
                </div>
                <div class="content">
                    <p>Gator description: {{description}}</p>
                </div>
                    <p>Created by {{user.userName}}on {{format_date date}}</p>
            </div>
        </div>
        
        <div class="add-comment">
            <input 


        </div>

       {{!-- comments.length (if there are comments in the array) and then use comment.comment, comment.user.username, comment.date etc or what the model uses --}}
        {{!-- <div class="comments">
            {{#if (comments.length)}}

            <p>{{comments.comment}}</p>
            console.log({{comments.comment}})
            <p>by {{comments.user.userName}} on {{comments.date}}</p>
            {{/if}}
        </div> --}}
       

        {{!-- <div id="edit-button" class="edit-button">
            <button type="submit" data-id="{{project.id}}">Edit</button>

        </div>

        <div class="delete-button">
            <button type="submit" data-id="{{project.id}}">Delete</button>
        </div> --}}

    </div>

    <script src="/js/dashboard.js"></script>